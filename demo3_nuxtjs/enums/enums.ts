export enum TypeType {
  Number = 'number',
  String = 'string'
}

export enum RequiredTypeEnum {
  Array = 'array'
}

export enum AffectedRowResponseRequired {
  Properties = 'properties',
  Required = 'required',
  Type = 'type'
}

export enum AffectedRowsType {
  Object = 'object'
}

export enum NewDeviceMetadataRequired {
  Ref = '$ref'
}

export enum UsersRequired {
  Items = 'items',
  Type = 'type'
}

export enum PropertiesRequiredEnum {
  List = 'list',
  Pagination = 'pagination'
}

export enum PurpleRequired {
  Example = 'example',
  Type = 'type'
}